"use client"

export default function CreditsPage() {
  return (
    <section className="surface fade-in p-8">
      <p className="text-xs uppercase tracking-[0.22em] text-[var(--muted-foreground)]">Contributors</p>
      <div className="mb-5 mt-2 h-2.5 w-56 rounded-full bg-[linear-gradient(90deg,var(--primary),transparent)]" />
      <h2 className="text-3xl font-semibold">Credits</h2>
      <p className="mt-2 text-sm text-[var(--muted-foreground)]">
        Content intentionally left empty for now.
      </p>
    </section>
  )
}
