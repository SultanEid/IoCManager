# AI Research Workspace

This folder contains research artifacts only.

- Notebooks under `ai/research/notebooks` are references for experimentation.
- Production service code in `ai/service` must not import or execute notebooks.
- Live-feed or interactive notebook runtime assumptions are not allowed in production paths.
