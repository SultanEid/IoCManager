# Copy to .env.local for local development.
# Required: backend base URL for API calls.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5127

# Runtime mode switch:
# 0 = demo mode (mock provider)
# 1 = aspnet mode (live ASP.NET gateway)
NEXT_PUBLIC_USE_ASPNET_GATEWAY=1

